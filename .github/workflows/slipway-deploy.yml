name: Slipway Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Notify Slipway build started
        run: |
          PAYLOAD='{"run_url":"'"$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"'"}'
          SIGNATURE=$(echo -n "$PAYLOAD" | openssl dgst -sha256 -hmac "a221d1ee176c1706c6abb35f4ab64af7a429ca3f32a268cfb0179927c7aaaf08" | awk '{print $2}')
          curl -s -X POST https://app.slipway.cloud/webhooks/build_started/blmubjl98rqhhkahmk0ml4ow \
            -H "Content-Type: application/json" \
            -H "X-Slipway-Signature: sha256=$SIGNATURE" \
            -d "$PAYLOAD" || true

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Dockerfile if missing
        run: |
          if [ ! -f Dockerfile ]; then
            echo "FROM ruby:3.2" > Dockerfile
            echo "WORKDIR /app" >> Dockerfile
            echo "COPY . ." >> Dockerfile
            echo 'CMD ["ruby", "app.rb"]' >> Dockerfile
          fi

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/pepito-c/resumesucks:latest
            ghcr.io/pepito-c/resumesucks:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Notify Slipway image ready
        run: |
          PAYLOAD='{"image":"ghcr.io/pepito-c/resumesucks:latest","sha":"'$GITHUB_SHA'"}'
          SIGNATURE=$(echo -n "$PAYLOAD" | openssl dgst -sha256 -hmac "a221d1ee176c1706c6abb35f4ab64af7a429ca3f32a268cfb0179927c7aaaf08" | awk '{print $2}')
          curl -f -s -X POST https://app.slipway.cloud/webhooks/image_ready/blmubjl98rqhhkahmk0ml4ow \
            -H "Content-Type: application/json" \
            -H "X-Slipway-Signature: sha256=$SIGNATURE" \
            -d "$PAYLOAD"
        # Secret is embedded — no GitHub Actions secret needed

      - name: Notify Slipway build failed
        if: failure()
        run: |
          PAYLOAD='{}'
          SIGNATURE=$(echo -n "$PAYLOAD" | openssl dgst -sha256 -hmac "a221d1ee176c1706c6abb35f4ab64af7a429ca3f32a268cfb0179927c7aaaf08" | awk '{print $2}')
          curl -s -X POST https://app.slipway.cloud/webhooks/build_failed/blmubjl98rqhhkahmk0ml4ow \
            -H "Content-Type: application/json" \
            -H "X-Slipway-Signature: sha256=$SIGNATURE" \
            -d "$PAYLOAD" || true
